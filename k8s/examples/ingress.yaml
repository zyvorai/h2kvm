# Example Ingress for h2kvm worker API
# Requires an Ingress controller (e.g., nginx, traefik)
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: h2kvm-api
  namespace: h2kvm-workers
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: "50g"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "3600"
spec:
  ingressClassName: nginx
  rules:
    - host: h2kvm.example.com
      http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: h2kvm-worker
                port:
                  number: 8080
          - path: /metrics
            pathType: Prefix
            backend:
              service:
                name: h2kvm-worker
                port:
                  number: 9090
