# Run with primary command (recommended):
#   h2kvmctl --config 99-merge-demos/merge-run-local.yaml
#
# Or using legacy command:
#   h2kvm --config 99-merge-demos/merge-run-local.yaml

command: local
vmdk: /path/to/vm.vmdk
to_output: merged-out.qcow2
report: merged.md
