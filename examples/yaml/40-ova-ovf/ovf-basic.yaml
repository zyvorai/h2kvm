# Run with primary command (recommended):
#   h2kvmctl --config 40-ova-ovf/ovf-basic.yaml
#
# Or using legacy command:
#   hyper2kvm --config 40-ova-ovf/ovf-basic.yaml

command: ovf
ovf: /path/to/appliance.ovf
flatten: true
to_output: appliance.qcow2
out_format: qcow2
compress: true
