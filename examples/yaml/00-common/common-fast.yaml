# Run with primary command (recommended):
#   h2kvmctl --config 00-common/common-fast.yaml
#
# Or using legacy command:
#   hyper2kvm --config 00-common/common-fast.yaml

output_dir: ./out
workdir: ./out/work
verbose: 1
regen_initramfs: false
remove_vmware_tools: false
flatten: false
libvirt_test: false
qemu_test: false
