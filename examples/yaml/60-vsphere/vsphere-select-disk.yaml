# Run with primary command (recommended):
#   h2kvmctl --config 60-vsphere/vsphere-select-disk.yaml
#
# Or using legacy command:
#   h2kvm --config 60-vsphere/vsphere-select-disk.yaml

command: vsphere
vcenter: vcenter.example.com
vc_user: administrator@vsphere.local
vc_password_env: VC_PASSWORD
vc_insecure: true
vs_action: select_disk
vm_name: myVM
label_or_index: Hard disk 1
json: true
