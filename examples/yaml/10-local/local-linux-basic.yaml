# Run with primary command (recommended):
#   h2kvmctl --config 10-local/local-linux-basic.yaml
#
# Or using legacy command:
#   h2kvm --config 10-local/local-linux-basic.yaml

command: local
vmdk: /path/to/linux.vmdk
to_output: linux-fixed.qcow2
report: local-linux-report.md
