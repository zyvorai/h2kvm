{{- if .Values.operator.metrics.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "h2kvm-operator.fullname" . }}
  namespace: {{ include "h2kvm-operator.namespace" . }}
  labels:
    {{- include "h2kvm-operator.operatorLabels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.operator.metrics.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "h2kvm-operator.operatorSelectorLabels" . | nindent 4 }}
{{- end }}
