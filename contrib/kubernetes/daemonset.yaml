apiVersion: v1
kind: Namespace
metadata:
  name: hyper2kvm-system
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: hyper2kvm-luks-unlocker
  namespace: hyper2kvm-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: hyper2kvm-luks-unlocker
rules:
- apiGroups: [""]
  resources: ["secrets"]
  verbs: ["get", "list"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: hyper2kvm-luks-unlocker
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: hyper2kvm-luks-unlocker
subjects:
- kind: ServiceAccount
  name: hyper2kvm-luks-unlocker
  namespace: hyper2kvm-system
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: hyper2kvm-luks-config
  namespace: hyper2kvm-system
data:
  luks.json: |
    {
      "tpm_handle": "0x81000010",
      "vault": {
        "addr": "https://vault.vault.svc:8200",
        "path": "secret/hyper2kvm/luks"
      }
    }
---
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: hyper2kvm-luks-unlocker
  namespace: hyper2kvm-system
  labels:
    app: hyper2kvm-luks-unlocker
spec:
  selector:
    matchLabels:
      app: hyper2kvm-luks-unlocker
  template:
    metadata:
      labels:
        app: hyper2kvm-luks-unlocker
    spec:
      serviceAccountName: hyper2kvm-luks-unlocker
      hostPID: true
      hostIPC: true
      containers:
      - name: unlocker
        image: hyper2kvm/luks-unlocker:latest
        imagePullPolicy: IfNotPresent

        securityContext:
          privileged: true
          capabilities:
            add:
            - SYS_ADMIN
            - SYS_MODULE
            - SYS_RAWIO

        env:
        - name: NODE_NAME
          valueFrom:
            fieldRef:
              fieldPath: spec.nodeName
        - name: VAULT_ADDR
          value: "https://vault.vault.svc:8200"
        - name: VAULT_TOKEN
          valueFrom:
            secretKeyRef:
              name: vault-token
              key: token
              optional: true

        volumeMounts:
        - name: dev
          mountPath: /dev
        - name: sys
          mountPath: /sys
        - name: config
          mountPath: /etc/hyper2kvm
          readOnly: true
        - name: run-cryptsetup
          mountPath: /run/cryptsetup
        - name: lib-modules
          mountPath: /lib/modules
          readOnly: true

        command:
        - /bin/sh
        - -c
        - |
          set -e
          echo "Hyper2KVM LUKS Unlocker starting on node: $NODE_NAME"

          # Load necessary kernel modules
          modprobe dm-crypt || true
          modprobe tpm || true
          modprobe tpm_tis || true

          # Run unlock
          hyper2kvm-luks unlock -v

          # Activate LVM volumes
          pvscan --cache
          vgchange -ay

          # Keep container running
          echo "Unlock complete. Monitoring for new devices..."
          while true; do
            sleep 3600
          done

        resources:
          requests:
            memory: "64Mi"
            cpu: "100m"
          limits:
            memory: "128Mi"
            cpu: "200m"

      volumes:
      - name: dev
        hostPath:
          path: /dev
          type: Directory
      - name: sys
        hostPath:
          path: /sys
          type: Directory
      - name: config
        configMap:
          name: hyper2kvm-luks-config
      - name: run-cryptsetup
        hostPath:
          path: /run/cryptsetup
          type: DirectoryOrCreate
      - name: lib-modules
        hostPath:
          path: /lib/modules
          type: Directory

      nodeSelector:
        # Only run on nodes with LUKS
        hyper2kvm.io/luks: "true"

      tolerations:
      - effect: NoSchedule
        operator: Exists
      - effect: NoExecute
        operator: Exists
