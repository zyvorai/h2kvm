# Generated Manifests Summary

## ✅ Manifests Successfully Generated

Generated on: 2026-02-17

### 1. CustomResourceDefinition (CRD)

**File**: `config/crd/bases/hyper2kvm.io_hyperconversions.yaml`
- **Size**: 422 lines, 18KB
- **API Version**: `apiextensions.k8s.io/v1`
- **Generated by**: controller-gen v0.16.5

**Key Features**:
- ✅ API Group: `hyper2kvm.io`
- ✅ Version: `v1alpha1`
- ✅ Kind: `HyperConversion`
- ✅ Short names: `hc`, `hconv`
- ✅ Scope: Namespaced
- ✅ Status subresource enabled
- ✅ Additional printer columns configured:
  - Phase (status.phase)
  - Progress (status.progress)
  - DataVolume (status.dataVolumeName)
  - VM (status.virtualMachineName)
  - Age (metadata.creationTimestamp)

**Schema Validation**:
- ✅ Source URL pattern validation (HTTP/HTTPS/S3)
- ✅ Disk format enum validation (vmdk, vdi, vhd, vhdx, qcow2, raw)
- ✅ Firmware enum validation (bios, uefi, uefi-secure)
- ✅ Network type enum validation (pod, bridge, sriov, multus)
- ✅ CPU cores range validation (1-128)
- ✅ Timeout range validation (5-1440 minutes)
- ✅ Default values applied:
  - source.format: qcow2
  - storage.accessMode: ReadWriteOnce
  - storage.volumeMode: Filesystem
  - vm.cpu.cores: 2
  - vm.cpu.sockets: 1
  - vm.cpu.threads: 1
  - vm.firmware: bios
  - vm.evictionStrategy: LiveMigrateIfPossible
  - vm.runStrategy: Always
  - vm.networks[].model: virtio
  - conversion.compression: zstd
  - conversion.timeout: 60

### 2. RBAC Manifests

**File**: `config/rbac/role.yaml`
- **Size**: 87 lines
- **Type**: ClusterRole
- **Name**: manager-role

**Permissions**:
- ✅ HyperConversions (hyper2kvm.io):
  - Full CRUD (create, delete, get, list, patch, update, watch)
  - Finalizers update
  - Status subresource (get, patch, update)

- ✅ DataVolumes (cdi.kubevirt.io):
  - Full CRUD for DataVolume management

- ✅ VirtualMachines (kubevirt.io):
  - Full CRUD for VM management

- ✅ Core resources:
  - Events: create, patch (for status updates)
  - Pods: Full CRUD (for Python worker jobs - future)

- ✅ Batch resources:
  - Jobs: Full CRUD (for conversion jobs - future)

**Other RBAC files**:
- `config/rbac/role_binding.yaml` - ClusterRoleBinding
- `config/rbac/service_account.yaml` - ServiceAccount

### 3. DeepCopy Code

**File**: `api/v1alpha1/zz_generated.deepcopy.go`
- **Size**: 8.9KB
- **Generated by**: controller-gen v0.16.5

**Generated methods**:
- ✅ DeepCopy for all types
- ✅ DeepCopyInto for all types
- ✅ DeepCopyObject for runtime.Object interface
- ✅ Covers all spec and status types:
  - HyperConversion
  - HyperConversionList
  - HyperConversionSpec
  - HyperConversionStatus
  - SourceSpec
  - StorageSpec
  - VMSpec
  - CPUSpec
  - NetworkSpec
  - CloudInitSpec
  - ConversionOptions
  - UploadProgressStatus

## CRD Schema Overview

### Spec Structure

```yaml
spec:
  source:              # Required
    url: string        # Required, pattern validated
    format: enum       # Default: qcow2
    checksum: string   # Optional
    secretRef:         # Optional
      name: string

  storage:             # Required
    storageClass: string     # Optional (uses cluster default)
    size: Quantity          # Optional (auto-detect)
    accessMode: enum        # Default: ReadWriteOnce
    volumeMode: enum        # Default: Filesystem

  vm:                  # Optional
    name: string       # Optional (defaults to CR name)
    cpu:               # Required if vm specified
      cores: int32     # Default: 2, range: 1-128
      sockets: int32   # Default: 1
      threads: int32   # Default: 1
    memory: Quantity   # Required if vm specified
    firmware: enum     # Default: bios
    networks:          # Optional (defaults to pod network)
    - name: string
      type: enum       # Default: pod
      networkName: string
      macAddress: string
      model: enum      # Default: virtio
    evictionStrategy: enum  # Default: LiveMigrateIfPossible
    runStrategy: enum       # Default: Always
    cloudInit:         # Optional
      userData: string
      userDataSecretRef:
        name: string
      networkData: string
      networkDataSecretRef:
        name: string

  conversion:          # Optional
    compression: enum  # Default: zstd
    offlineFixes: bool # Default: false
    timeout: int32     # Default: 60, range: 5-1440
```

### Status Structure

```yaml
status:
  phase: enum         # Pending, Uploading, Converting, CreatingVM, Ready, Failed
  progress: int32     # 0-100
  dataVolumeName: string
  virtualMachineName: string
  uploadProgress:
    bytesUploaded: int64
    totalBytes: int64
    speed: int64
    lastUpdateTime: timestamp
  startTime: timestamp
  completionTime: timestamp
  conditions:
  - type: string
    status: enum      # True, False, Unknown
    reason: string
    message: string
    lastTransitionTime: timestamp
  message: string
```

## Validation Summary

### Pattern Validations
- Source URL: `^https?://.*$|^s3://.*$`
- Storage size: Kubernetes resource.Quantity format

### Enum Validations
- source.format: vmdk, vdi, vhd, vhdx, qcow2, raw
- storage.accessMode: ReadWriteOnce, ReadWriteMany, ReadOnlyMany
- storage.volumeMode: Block, Filesystem
- vm.firmware: bios, uefi, uefi-secure
- vm.networks[].type: pod, bridge, sriov, multus
- vm.networks[].model: virtio, e1000, e1000e, rtl8139
- vm.evictionStrategy: LiveMigrate, LiveMigrateIfPossible, None
- vm.runStrategy: Always, RerunOnFailure, Manual, Halted
- conversion.compression: zstd, zlib, none

### Range Validations
- vm.cpu.cores: 1-128
- vm.cpu.sockets: minimum 1
- vm.cpu.threads: minimum 1
- conversion.timeout: 5-1440

### Required Fields
- spec.source (required)
  - spec.source.url (required)
- spec.storage (required)
- spec.vm.cpu (required if vm specified)
  - spec.vm.cpu.cores (required)
- spec.vm.memory (required if vm specified)
- spec.vm.networks[].name (required if networks specified)

## Usage Examples

### Install CRD

```bash
# Install to cluster
kubectl apply -f config/crd/bases/hyper2kvm.io_hyperconversions.yaml

# Verify installation
kubectl get crd hyperconversions.hyper2kvm.io

# View CRD details
kubectl describe crd hyperconversions.hyper2kvm.io
```

### Create HyperConversion

```bash
# Apply sample CR
kubectl apply -f config/samples/simple-vmdk-to-vm.yaml

# Watch status with custom columns
kubectl get hyperconversion -w

# Expected output:
# NAME              PHASE      PROGRESS   DATAVOLUME           VM           AGE
# simple-vmdk-...   Pending    0                                           1s
# simple-vmdk-...   Uploading  5          simple-vmdk-...-dv               10s
# simple-vmdk-...   Ready      100        simple-vmdk-...-dv   simple-...  2m
```

### Validate CRD Schema

```bash
# Test validation (should fail - invalid format)
kubectl apply -f - <<EOF
apiVersion: hyper2kvm.io/v1alpha1
kind: HyperConversion
metadata:
  name: test-validation
spec:
  source:
    url: "https://example.com/disk.img"
    format: invalid-format  # Should fail enum validation
  storage:
    size: 20Gi
  vm:
    cpu:
      cores: 2
    memory: 4Gi
EOF

# Error expected:
# The HyperConversion "test-validation" is invalid:
# spec.source.format: Unsupported value: "invalid-format":
# supported values: "vmdk", "vdi", "vhd", "vhdx", "qcow2", "raw"
```

## Next Steps

1. **Deploy Operator**:
   ```bash
   make deploy IMG=hyper2kvm-operator:latest
   ```

2. **Install CRD** (if not using `make deploy`):
   ```bash
   make install
   ```

3. **Test with samples**:
   ```bash
   kubectl apply -f config/samples/simple-vmdk-to-vm.yaml
   kubectl get hc -w
   ```

4. **View logs**:
   ```bash
   kubectl logs -n hyper2kvm-system -l control-plane=controller-manager -f
   ```

## Files Generated

```
config/
├── crd/
│   └── bases/
│       └── hyper2kvm.io_hyperconversions.yaml  ✅ (422 lines, 18KB)
├── rbac/
│   └── role.yaml                               ✅ (87 lines, updated)
api/
└── v1alpha1/
    └── zz_generated.deepcopy.go                ✅ (8.9KB)
```

## Verification Commands

```bash
# Verify CRD syntax
kubectl apply --dry-run=client -f config/crd/bases/hyper2kvm.io_hyperconversions.yaml

# Verify RBAC
kubectl apply --dry-run=client -f config/rbac/

# Check Go code compiles
make build

# Run tests
make test

# Generate again (should be idempotent)
make manifests generate
```

## Summary

✅ All manifests generated successfully
✅ CRD includes comprehensive validation
✅ RBAC includes all required permissions
✅ DeepCopy code generated for all types
✅ Schema includes proper defaults
✅ Status subresource enabled
✅ Printer columns configured

The operator is ready for deployment!
