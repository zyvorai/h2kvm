apiVersion: v1
kind: Namespace
metadata:
  name: h2kvm-system
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: h2kvm-operator
  namespace: h2kvm-system
  labels:
    app: h2kvm-operator
spec:
  replicas: 1
  selector:
    matchLabels:
      app: h2kvm-operator
  template:
    metadata:
      labels:
        app: h2kvm-operator
    spec:
      serviceAccountName: h2kvm-operator
      containers:
        - name: manager
          image: h2kvm/operator:latest
          imagePullPolicy: IfNotPresent
          command:
            - /manager
          args:
            - --leader-elect
          env:
            - name: VALIDATION_IMAGE
              value: "h2kvm/validator:latest"
            - name: FIXER_IMAGE
              value: "h2kvm/fixer:latest"
            - name: VALIDATION_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
          resources:
            limits:
              cpu: 500m
              memory: 512Mi
            requests:
              cpu: 100m
              memory: 128Mi
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8081
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8081
            initialDelaySeconds: 5
            periodSeconds: 10
